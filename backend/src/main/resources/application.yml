spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/case-management
    username: sorsix
    password: sorsix


    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration